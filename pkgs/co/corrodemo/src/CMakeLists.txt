cmake_minimum_required(VERSION 3.22)
project(corrodemo C)
find_package(Corrosion REQUIRED)
corrosion_import_crate(MANIFEST_PATH rs/Cargo.toml)
add_executable(corrodemo main.c)
target_link_libraries(corrodemo PRIVATE adder)
install(TARGETS corrodemo)
enable_testing()
add_test(NAME runs COMMAND corrodemo)
