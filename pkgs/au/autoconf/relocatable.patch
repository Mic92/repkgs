--- a/bin/autoconf.in
+++ b/bin/autoconf.in
@@ -27,9 +27,12 @@
 use strict;
 use warnings FATAL => 'all';
 
+our $pkgs_prefix;
 BEGIN
 {
-  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || '@pkgdatadir@';
+  use Cwd ();
+  ($pkgs_prefix = Cwd::realpath ($0)) =~ s,/[^/]*/[^/]*$,,;
+  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || "$pkgs_prefix/share/autoconf";
   unshift @INC, $pkgdatadir;
 
   # Override SHELL.  On DJGPP SHELL may not be set to a shell
@@ -44,8 +47,8 @@
 use Autom4te::General;
 
 # Lib files.
-my $autom4te = $ENV{'AUTOM4TE'} || '@bindir@/@autom4te-name@';
-my $trailer_m4 = $ENV{'trailer_m4'} || '@pkgdatadir@/autoconf/trailer.m4';
+my $autom4te = $ENV{'AUTOM4TE'} || "$pkgs_prefix/bin/@autom4te-name@";
+my $trailer_m4 = $ENV{'trailer_m4'} || "$pkgs_prefix/share/autoconf/" . 'autoconf/trailer.m4';
 
 # $HELP
 # -----
--- a/bin/autoheader.in
+++ b/bin/autoheader.in
@@ -30,9 +30,12 @@
 use strict;
 use warnings FATAL => 'all';
 
+our $pkgs_prefix;
 BEGIN
 {
-  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || '@pkgdatadir@';
+  use Cwd ();
+  ($pkgs_prefix = Cwd::realpath ($0)) =~ s,/[^/]*/[^/]*$,,;
+  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || "$pkgs_prefix/share/autoconf";
   unshift @INC, "$pkgdatadir";
 
   # Override SHELL.  On DJGPP SHELL may not be set to a shell
@@ -54,7 +57,7 @@
 our ($config_h, %symbol, %verbatim);
 
 # Lib files.
-my $autom4te = $ENV{'AUTOM4TE'} || '@bindir@/@autom4te-name@';
+my $autom4te = $ENV{'AUTOM4TE'} || "$pkgs_prefix/bin/@autom4te-name@";
 my $config_h_in;
 my @prepend_include;
 my @include;
--- a/bin/autom4te.in
+++ b/bin/autom4te.in
@@ -26,9 +26,12 @@
 use strict;
 use warnings FATAL => 'all';
 
+our $pkgs_prefix;
 BEGIN
 {
-  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || '@pkgdatadir@';
+  use Cwd ();
+  ($pkgs_prefix = Cwd::realpath ($0)) =~ s,/[^/]*/[^/]*$,,;
+  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || "$pkgs_prefix/share/autoconf";
   unshift @INC, $pkgdatadir;
 
   # Override SHELL.  On DJGPP SHELL may not be set to a shell
@@ -48,7 +51,7 @@
 use Autom4te::XFile;
 
 # Data directory.
-my $pkgdatadir = $ENV{'AC_MACRODIR'} || '@pkgdatadir@';
+my $pkgdatadir = $ENV{'AC_MACRODIR'} || "$pkgs_prefix/share/autoconf";
 
 # $LANGUAGE{LANGUAGE} -- Automatic options for LANGUAGE.
 my %language;
@@ -321,6 +324,7 @@
       next
 	if /^\s*(\#.*)?$/;
 
+      s/\@pkgs_pkgdatadir\@/$pkgdatadir/g;
       my @words = shellwords ($_);
       my $type = shift @words;
       if ($type eq 'begin-language:')
--- a/bin/autoreconf.in
+++ b/bin/autoreconf.in
@@ -30,9 +30,12 @@
 use warnings FATAL => 'all';
 
 my $buildauxdir;
+our $pkgs_prefix;
 BEGIN
 {
-  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || '@pkgdatadir@';
+  use Cwd ();
+  ($pkgs_prefix = Cwd::realpath ($0)) =~ s,/[^/]*/[^/]*$,,;
+  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || "$pkgs_prefix/share/autoconf";
   unshift @INC, $pkgdatadir;
 
   $buildauxdir = $ENV{'autom4te_buildauxdir'} || $pkgdatadir . '/build-aux';
@@ -125,9 +128,9 @@
 # Programs we can run.
 # For each program, the value is the actual command to use, with options.
 my %C = (
-  autoconf    => $ENV{'AUTOCONF'}    || '@bindir@/@autoconf-name@',
-  autoheader  => $ENV{'AUTOHEADER'}  || '@bindir@/@autoheader-name@',
-  autom4te    => $ENV{'AUTOM4TE'}    || '@bindir@/@autom4te-name@',
+  autoconf    => $ENV{'AUTOCONF'}    || "$pkgs_prefix/bin/@autoconf-name@",
+  autoheader  => $ENV{'AUTOHEADER'}  || "$pkgs_prefix/bin/@autoheader-name@",
+  autom4te    => $ENV{'AUTOM4TE'}    || "$pkgs_prefix/bin/@autom4te-name@",
   automake    => $ENV{'AUTOMAKE'}    || 'automake',
   aclocal     => $ENV{'ACLOCAL'}     || 'aclocal',
   libtoolize  => $ENV{'LIBTOOLIZE'}  || 'libtoolize',
--- a/bin/autoscan.in
+++ b/bin/autoscan.in
@@ -28,9 +28,12 @@
 use strict;
 use warnings FATAL => 'all';
 
+our $pkgs_prefix;
 BEGIN
 {
-  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || '@pkgdatadir@';
+  use Cwd ();
+  ($pkgs_prefix = Cwd::realpath ($0)) =~ s,/[^/]*/[^/]*$,,;
+  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || "$pkgs_prefix/share/autoconf";
   unshift @INC, $pkgdatadir;
 
   # Override SHELL.  On DJGPP SHELL may not be set to a shell
@@ -95,10 +98,10 @@
 my $log;
 
 # Autoconf and lib files.
-my $autom4te = $ENV{'AUTOM4TE'} || '@bindir@/@autom4te-name@';
+my $autom4te = $ENV{'AUTOM4TE'} || "$pkgs_prefix/bin/@autom4te-name@";
 my $autoconf = "$autom4te --language=autoconf";
 my @prepend_include;
-my @include = ('@pkgdatadir@');
+my @include = ("$pkgs_prefix/share/autoconf");
 
 # $help
 # -----
--- a/bin/autoupdate.in
+++ b/bin/autoupdate.in
@@ -29,9 +29,12 @@
 use strict;
 use warnings FATAL => 'all';
 
+our $pkgs_prefix;
 BEGIN
 {
-  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || '@pkgdatadir@';
+  use Cwd ();
+  ($pkgs_prefix = Cwd::realpath ($0)) =~ s,/[^/]*/[^/]*$,,;
+  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || "$pkgs_prefix/share/autoconf";
   unshift @INC, $pkgdatadir;
 
   # Override SHELL.  On DJGPP SHELL may not be set to a shell
@@ -51,11 +54,11 @@
 use Autom4te::XFile;
 
 # Lib files.
-my $autom4te = $ENV{'AUTOM4TE'} || '@bindir@/@autom4te-name@';
+my $autom4te = $ENV{'AUTOM4TE'} || "$pkgs_prefix/bin/@autom4te-name@";
 my $autoconf = "$autom4te --language=autoconf";
 # We need to find m4sugar.
 my @prepend_include;
-my @include = ('@pkgdatadir@');
+my @include = ("$pkgs_prefix/share/autoconf");
 my $force = 0;
 # m4.
 my $m4 = $ENV{"M4"} || '@M4@';
--- a/bin/ifnames.in
+++ b/bin/ifnames.in
@@ -33,9 +33,12 @@
 use strict;
 use warnings FATAL => 'all';
 
+our $pkgs_prefix;
 BEGIN
 {
-  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || '@pkgdatadir@';
+  use Cwd ();
+  ($pkgs_prefix = Cwd::realpath ($0)) =~ s,/[^/]*/[^/]*$,,;
+  my $pkgdatadir = $ENV{'autom4te_perllibdir'} || "$pkgs_prefix/share/autoconf";
   unshift @INC, $pkgdatadir;
 
   # Override SHELL.  On DJGPP SHELL may not be set to a shell
--- a/lib/autom4te.in
+++ b/lib/autom4te.in
@@ -124,7 +124,7 @@
 # This intermediate language is used by aclocal to build aclocal.m4.
 
 begin-language: "Autoconf-without-aclocal-m4"
-args: --prepend-include '@pkgdatadir@'
+args: --prepend-include '@pkgs_pkgdatadir@'
 args: --cache=autom4te.cache
 args: autoconf/autoconf.m4f
 args: acsite.m4?
@@ -151,7 +151,7 @@
 ## -------- ##
 
 begin-language: "Autotest"
-args: --prepend-include '@pkgdatadir@'
+args: --prepend-include '@pkgs_pkgdatadir@'
 args: autotest/autotest.m4f
 args: package.m4?
 args: local.at?
@@ -165,7 +165,7 @@
 ## ---- ##
 
 begin-language: "M4sh"
-args: --prepend-include '@pkgdatadir@'
+args: --prepend-include '@pkgs_pkgdatadir@'
 args: m4sugar/m4sh.m4f
 args: --mode 777
 args: --language M4sugar
@@ -177,6 +177,6 @@
 ## ------- ##
 
 begin-language: "M4sugar"
-args: --prepend-include '@pkgdatadir@'
+args: --prepend-include '@pkgs_pkgdatadir@'
 args: m4sugar/m4sugar.m4f
 end-language: "M4sugar"
